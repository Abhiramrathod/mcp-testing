<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.abhiramrathod</groupId>
        <artifactId>mcpTesting</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>mcp-test-transport</artifactId>
    <name>MCP Test Transport</name>

    <properties>
        <maven.deploy.skip>false</maven.deploy.skip>
        <!-- 4.1.125.Final+ fixes CVE-2025-46903 (BrotliDecoder OOM DoS);
             4.1.133.Final+ fixes CVE-2026-42581 (HTTP/1.0 request smuggling);
             4.1.135.Final+ fixes CVE-2026-47691 (DNS cache poisoning);
             4.1.136.Final+ fixes CVE-2026-56817 (XXE), CVE-2026-44891, CVE-2026-55833;
             4.1.137.Final fixes CVE-2026-59901/59902/59903 (handler/codec-http/SCTP). -->
        <netty.version>4.2.17.Final</netty.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.github.abhiramrathod</groupId>
            <artifactId>mcp-test-interfaces</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.abhiramrathod</groupId>
            <artifactId>mcp-test-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-handler</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-codec-http</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-handler-proxy</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>net.nicoulaj.maven.plugins</groupId>
                <artifactId>checksum-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
